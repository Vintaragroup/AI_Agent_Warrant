---
description: Specification for routing and managing call transfers between agents and systems
---


# transfer-system-algorithms

From the provided specification files, the transfer system appears to be primarily focused on compliance-related call routing rather than standard agent-to-agent transfers. The key components include:

## Compliance Verification Transfer Flow (Importance Score: 85/100)
Located in `app/main.py`, this component manages:
- Routing of compliance verification calls based on bond status
- Two-step verification integration into transfer paths
- Token-based routing for secure compliance handoffs

## Location-Aware Transfer Logic (Importance Score: 75/100)
Implemented in `app/geo.py`, this system:
- Routes transfers based on geographic compliance zones
- Validates transfer destinations against approved locations
- Handles location-based compliance verification during transfers

## Notification Integration (Importance Score: 65/100)
Found in `app/sms.py`, this module:
- Manages transfer notifications across multiple providers
- Coordinates verification messages during transfer workflows
- Handles media attachments for compliance documentation

The transfer system appears tightly integrated with the compliance verification workflow, focusing on secure handoffs and location-based routing rather than traditional call center transfer patterns.

$END$

 If you're using this file in context, clearly say in italics in one small line that "Context added by Giga transfer-system-algorithms" along with specifying exactly what information was used from this file in a human-friendly way, instead of using kebab-case use normal sentence case.